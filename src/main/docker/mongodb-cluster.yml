version: '2'
services:
    kitchen-mongodb:
        image: mongo:3.4.10
        ports:
            - "27017:27017"
        command: mongos --configdb kitchen-mongodb-config
    kitchen-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    kitchen-mongodb-config:
        image: mongo:3.4.10
        container_name: kitchen-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
